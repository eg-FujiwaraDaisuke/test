import 'package:flutter/material.dart';

class SupportHistoryPowerPlantPage extends StatelessWidget {
  const SupportHistoryPowerPlantPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
